<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.winnyking.wincalendar">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" /> --> <!-- Not needed for HelloWorld -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light"> <!-- Using a standard theme -->

        <activity android:name="com.winnyking.wincalendar.ActivityMain" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- HTML Viewer Plugin Components -->
        <activity
            android:name="com.winnyking.wincalendar.tasker.htmlviewer.HtmlViewerActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/html_viewer_label_config">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
            <meta-data
                android:name="com.twofortyfouram.locale.intent.extra.BLURB"
                android:value="@string/html_viewer_blurb" />
        </activity>
        <activity
            android:name="com.winnyking.wincalendar.tasker.htmlviewer.WebViewActivity"
            android:exported="false" />

    </application>
</manifest>
